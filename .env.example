# ─────────────────────────────────────────────────────────────────────────
# Ropofy — renderizador de propuestas · variables de entorno
# Copia este archivo a .env.local y completa lo que necesites. Todo es
# OPCIONAL: sin nada configurado, la app corre con almacenamiento en memoria
# (respaldado por ./.data en local) y sin webhook.
# ─────────────────────────────────────────────────────────────────────────

# Webhook de aceptación (la conexión con n8n u otro).
# Si está vacío, al aceptar se registra localmente y NO se envía nada.
# Si está definido, al aceptar se hace POST con reintentos (fire-and-forget):
# una falla del webhook nunca bloquea la confirmación del cliente.
ACCEPTANCE_WEBHOOK_URL=

# Almacenamiento — Vercel KV (Upstash Redis).
# Si ambas están definidas, la app usa KV automáticamente; si no, usa el
# almacenamiento en memoria + archivo local. La interfaz de almacenamiento es
# la misma, así que mañana se cambia a Postgres sin tocar la UI.
KV_REST_API_URL=
KV_REST_API_TOKEN=
